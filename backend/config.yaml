# backend/config.yaml
# Trader 配置已移至 data/traders/{name}/trader.json
# 通过 CLI 或 REST API 创建 trader

data_sources:
  CN: akshare
  HK: yfinance
  US: yfinance

logging:
  level: INFO
  file: "data/logs/tradecraft.log"

# AI Agent 配置
# type 可选值: kiro | codex | claude
ai_agent:
  type: codex
